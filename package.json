{
  "name": "aury-web-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Aury Web monorepo",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "build:cli": "cd packages/cli && pnpm build",
    "build:core": "cd packages/core && pnpm build",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean",
    "release:cli": "cd packages/cli && pnpm release",
    "release:core": "cd packages/core && pnpm release"
  },
  "author": "Aury Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/AuriMyth/create-aury-web"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "pnpm@10.13.0",
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
