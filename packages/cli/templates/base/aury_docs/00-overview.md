# 00 - 项目总览

## 项目介绍

{{PROJECT_NAME}} 是一个基于 **React 19** 和 **Vite 6** 的现代化前端项目，采用 **Cyberpunk / Glitch** 视觉风格，集成了业界最新最强大的技术栈。

## 核心特性

### 🚀 技术栈
- **React 19**: 最新版本，支持 Server Components (future)
- **Vite 6**: 极速构建工具，ESM 原生支持
- **TypeScript 5.7+**: 完整类型安全
- **TanStack Router v1**: File-based 路由，完全类型安全
- **TanStack Query v5**: 服务端状态管理
- **Zustand v5**: 轻量全局状态
- **Tailwind CSS v4**: 使用 Lightning CSS 引擎
- **Shadcn UI**: 预装 15 个核心组件

### 🎨 设计系统
- **Cyberpunk / Glitch 主题**: 霓虹色、扫描线、故障效果
- **OKLCH 色彩空间**: 更广色域、更好的感知均匀性
- **响应式设计**: 移动优先
- **暗色模式**: 默认暗色，支持明暗切换
- **动画效果**: 细腻的悬停、过渡、视觉反馈

### 📦 预装组件 (Shadcn UI)
项目已预装 15 个核心 UI 组件，开箱即用：

1. **Button** - 按钮组件 (多种变体)
2. **Card** - 卡片容器
3. **Input** - 输入框
4. **Label** - 标签
5. **Form** - 表单容器 (集成 React Hook Form)
6. **Select** - 下拉选择
7. **Checkbox** - 复选框
8. **Switch** - 开关
9. **Dialog** - 对话框/模态框
10. **Tabs** - 标签页
11. **Sheet** - 侧边抽屉
12. **Toast** - 提示通知
13. **Alert** - 警告提示
14. **Badge** - 徽章标签
15. **Table** - 表格

### 🛠 开发工具
- **React Hook Form + Zod**: 表单验证
- **Axios**: HTTP 请求封装
- **Lucide React**: 图标库
- **Class Variance Authority**: 组件样式变体管理
- **TanStack Router Devtools**: 路由调试工具

## 项目目标

本项目旨在提供：
1. **快速启动**: 开箱即用的完整开发环境
2. **最佳实践**: 遵循 React 19 和现代前端最佳实践
3. **类型安全**: 全栈 TypeScript，端到端类型安全
4. **开发体验**: 热更新、调试工具、清晰的代码结构
5. **AI 友好**: 详细文档，方便 AI Agent 理解和开发

## 快速开始

### 安装依赖
```bash
pnpm install
```

### 启动开发服务器
```bash
pnpm dev
# 访问 http://localhost:3000
```

### 构建生产版本
```bash
pnpm build
```

### 类型检查
```bash
pnpm typecheck
```

### 代码检查
```bash
pnpm lint
```

## 项目架构

### 技术架构层次
```
┌─────────────────────────────────────────┐
│         UI Layer (React Components)      │
│  - Shadcn UI Components                  │
│  - Custom Cyberpunk Components           │
│  - Layout Components                     │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│      State Management Layer              │
│  - TanStack Query (Server State)         │
│  - Zustand (Global State)                │
│  - React Hook Form (Form State)          │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│         Routing Layer                    │
│  - TanStack Router (File-based)          │
│  - Type-safe Navigation                  │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│         API Layer                        │
│  - Axios HTTP Client                     │
│  - Request/Response Interceptors         │
│  - BaseResponse Standardization          │
└─────────────────────────────────────────┘
```

### 代码组织原则
1. **按功能模块组织** (features/)
2. **共享组件分离** (components/)
3. **类型定义集中** (types/)
4. **工具函数独立** (lib/, hooks/)
5. **路由文件驱动** (routes/)

## 环境配置

### 环境变量
项目使用 `.env` 文件管理环境配置：

```bash
# API 配置
VITE_API_BASE_URL=http://localhost:3000/api/v1

# 应用配置
VITE_APP_TITLE={{PROJECT_NAME}}
VITE_APP_VERSION=0.1.0

# 功能开关
VITE_ENABLE_DEVTOOLS=true
```

### 多环境支持
- `.env.development` - 开发环境
- `.env.production` - 生产环境
- `.env.local` - 本地覆盖 (git ignore)

## 开发规范

### 代码风格
- **使用 TypeScript**: 所有文件必须有类型定义
- **使用 ESM**: `import/export` 而非 `require`
- **使用箭头函数**: 组件、工具函数统一使用箭头函数
- **使用 const**: 优先使用 `const`，避免 `var`

### 命名规范
- **组件**: PascalCase (e.g., `Button`, `UserProfile`)
- **文件**: kebab-case (e.g., `user-profile.tsx`)
- **函数/变量**: camelCase (e.g., `getUserData`)
- **类型**: PascalCase (e.g., `User`, `ApiResponse`)
- **常量**: UPPER_SNAKE_CASE (e.g., `API_BASE_URL`)

### 文件组织
```typescript
// 1. 导入第三方库
import React from 'react'
import { useQuery } from '@tanstack/react-query'

// 2. 导入项目内部模块
import { Button } from '@/components/ui/button'
import { useAuth } from '@/hooks/useAuth'

// 3. 导入类型
import type { User } from '@/types'

// 4. 组件定义
export function UserProfile() {
  // ...
}
```

## 性能优化策略

1. **路由懒加载**: 按需加载路由组件
2. **代码分割**: Vite 自动分割 chunks
3. **Tree Shaking**: 自动移除未使用代码
4. **图片优化**: 使用 WebP、懒加载
5. **缓存策略**: TanStack Query 智能缓存

## 部署方案

### Docker 部署
```bash
docker build -t {{PROJECT_NAME}} .
docker run -p 80:80 {{PROJECT_NAME}}
```

### 静态托管
构建产物位于 `dist/` 目录，可直接部署到：
- Vercel
- Netlify
- AWS S3 + CloudFront
- Nginx

## 后续文档导航

- **[01-project-structure.md](./01-project-structure.md)** - 目录结构详解
- **[02-components.md](./02-components.md)** - 组件开发指南
- **[03-state-management.md](./03-state-management.md)** - 状态管理详解
- **[04-routing.md](./04-routing.md)** - 路由系统详解
- **[05-api-client.md](./05-api-client.md)** - API 请求详解
- **[06-styling.md](./06-styling.md)** - 样式系统详解
- **[07-forms.md](./07-forms.md)** - 表单处理详解
- **[08-hooks.md](./08-hooks.md)** - Hooks 使用详解
- **[99-best-practices.md](./99-best-practices.md)** - 最佳实践

---

**建议**: 开发前先阅读 AGENTS.md 和本系列文档，了解项目架构和开发规范。
