# API 配置
# 只配置域名和端口，API 路径前缀 (/api/v1) 在代码中统一管理
VITE_API_BASE_URL=http://localhost:8000
VITE_API_TIMEOUT=15000

# 应用配置
VITE_APP_TITLE={{PROJECT_NAME}}

# Mock 配置 (开发环境)
# 启用后将使用 MSW 拦截请求，无需后端服务
VITE_ENABLE_MOCK=false
